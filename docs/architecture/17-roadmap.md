# KEEL — Implementation Roadmap (Phases 0–15)

> Document 17 · Status: FROZEN — Architecture v1.0 (2026-07-12)
> Complexity scale: S / M / L / XL. Every phase's exit criteria include: CI green on 3 OS, docs updated, no new dependency-rule violations. Phases are sequential unless noted; each maps to the metrics in Doc 00 §3.

| Ph | Objective | Scope & Deliverables | Acceptance / Exit criteria | Cx | Depends |
|----|-----------|----------------------|----------------------------|----|---------|
| **0** | Foundations | Workspace scaffold (2 pkgs: `keel`, `@keel/runner-sdk`), tsconfig/eslint/depcruise rules from Doc 03, tiered CI skeleton (fast lane <5 min on PR; full matrix on merge/nightly), ADR files seeded (001–016), error hierarchy, ULIDs, Logger port | depcruise forbidden-edge rules failing on a deliberate violation (proof the gate works); hello-world CLI ships via release pipeline end-to-end | S | — |
| **1** | Behavior Model | `model/`: entities, canonical serializer, hashing, golden serialization tests, property tests (stability, idempotence) | canonical bytes identical across 3 OS in CI; property suites pass with large budgets | M | 0 |
| **2** | Execution engine (command runner) | `Runner` port + registry, subprocess sandbox (shadow workdir, env allowlist, caps, AbortSignal), group-kill on **all tier-1 platforms** (POSIX process groups + Windows Job Objects — moved here from P10 at freeze), streaming sink, fs-manifest observation, runner contract-test kit v0 | contract kit passes; cancellation ≤ grace budget; runaway-output fixture truncated live | L | 1 |
| **3** | Storage | SQLite schema+migrations, CAS with atomic writes, repositories + contract tests, advisory lock, `doctor` integrity checks | crash-injection tests (kill during capture) never yield visible partial state | M | 1 |
| **4** | Capture + verification | Capture engine, normalization ruleset v1 (volatile scrubbing, JSON sniffing, secret detectors), baseline seal/reject flow, config system (schema, hierarchy, hash) | flaky fixture is *rejected* with flapping path named; secrets fixture scrubbed; determinism gate (capture→replay ×20) green | L | 2,3 |
| **5** | Replay + diff + verdict → **MVP** | Replay engine with provenance validation, pure diff (taxonomy, comparators, identity keys, hash short-circuit), verdict assembly+persistence, CLI (`init/capture/check/report/baseline`), exit-code contract; **KEEL begins dogfooding itself** | reference repo: check ≤30s; regression corpus v0 recall ≥90%; `diff(s,s)=[]` property green; stale-baseline path exercised e2e | L | 4 |
| **6** | MCP server | stdio adapter, 5 tools (Doc 09), diff-scoped `keel_check` default (path-prefix heuristic, sound over-approximation), progress notifications, golden schema tests, verdict-format reference doc, busy/lock semantics, agent-loop example | full session against a real MCP host on 3 OS; schema lockfile in CI | M | 5 |
| **7** | Node deep runner | preload interceptors (clock, RNG, TZ/locale; network record/stub), side-channel fd protocol, interceptor versioning in fingerprint | node fixture with `Date.now`+`fetch` passes determinism gate; unrecorded-effect divergence demonstrated | L | 2,5 |
| **8** | Heuristic classification | classify module skeleton, rule registry, initial rule set, annotations persistence (facts-first ordering), suppressions (`keel_suppress`, stableId joins) | eval corpus v0: heuristic precision ≥95% on its claimed subset; AI-deletable CI job green (trivially — no AI yet) | M | 5 |
| **9** | LLM classification → **v1 feature-complete** | `InferenceProvider` port, Ollama provider (loopback-enforced), evidence packets, template versioning, budget/circuit-breaker/fallback ladder, zero-egress CI test | classification precision ≥80% on eval corpus; every fallback path visibly annotated; check-with-LLM ≤90s budget | L | 8 |
| **10** | Hardening & platform parity | Windows encoding/path edge-case audit, second inference provider (llama.cpp-server) validating the port, `doctor --bundle`, error-message audit vs Doc 10 C2 | provider swap requires zero classify-module changes; Windows determinism gate at parity | M | 6,7,9 |
| **11** | DX & docs push → **v1.0 release** | `keel init` proposal flow (repo inspection), `keel_probe_propose` MCP tool, guides ("writing good probes", agent integration), flagship scripted demo, examples CI-smoked, README non-goals, `tests/bench` extracted as `@keel/bench`, GOVERNANCE.md | new-user first-verdict ≤3 min (tested); 1.0 gate from Doc 14 (determinism held a full cycle, store round-trip) | M | 10 |
| **12** | Replay skipping | probe→file dependency map from Node runner module-graph capture; diff-intersection scheduling; sound fallback to full replay | touched-nothing edit: check cost ≈ hash comparisons only; soundness property tested (never skips an affected probe) | L | 7,11 |
| **13** | Opt-in OS sandboxing | sandbox-exec (macOS) / nsjail-or-landlock (Linux) / AppContainer (Windows) as *opt-in* runner wrappers; honest docs update | sandboxed runs pass contract kit; no default-path behavior change | L | 10 |
| **14** | Watch mode & agent-loop ergonomics | long-lived session mode, incremental re-check on change events, verdict deltas ("newly diverged since last check") | agent loop demo: sub-5s re-check on single-probe change (with P12 skipping) | M | 12 |
| **15** | Ecosystem opening | runner-sdk 1.0 (frozen contract + kit), Python deep-runner reference plugin, community rule/normalization contribution pipeline, plugin trust docs | external plugin passes kit without core patches; first community normalization rules merged | XL | 11,13 |
